{
  "name": "17-zk-snarks-privacy",
  "version": "0.1.0",
  "private": true,
  "description": "Toolchain Circom/SnarkJS para el Privacy Pool (modulo 17). Contratos via Foundry.",
  "scripts": {
    "compile:circuit": "node scripts/compile-circuit.mjs",
    "generate:proof": "node scripts/generate-proof.mjs",
    "export:verifier": "node scripts/export-verifier.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {}
}
