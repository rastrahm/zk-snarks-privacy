# Foundry
cache/
out/
broadcast/

# Dependencias Foundry (forge install)
/lib/

# Variables de entorno y secretos
.env
.env.*
!.env.example
!.env*.example

# Claves / wallets (nunca versionar)
*.pem
*.key
keystore/
wallets/
secrets/

# Ceremonias / toxic waste (nunca versionar)
*_0000.ptau
*_final.ptau
*.ptau
!circuits/pot*.ptau.example
powersOfTau*
ceremony/
zkeys/*.zkey
!zkeys/.gitkeep
verification_key.json
# Permitir VKs de test versionadas si se documentan explícitamente:
# circuits/build/**/verification_key.json puede regenerarse; no commitear zkeys de producción

# Circom / SnarkJS artefactos de build
circuits/build/
circuits/**/*.wasm
circuits/**/*.r1cs
circuits/**/*.sym
circuits/**/*.wtns
circuits/**/*_js/
*.circom.lock

# Cobertura y reportes
lcov.info
coverage/
gas-report.txt
gas-snapshot

# Frontend (Next.js) — fuera de alcance v1
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/
frontend/.turbo/
frontend/.vercel/
frontend/next-env.d.ts
frontend/*.tsbuildinfo
frontend/coverage/

# Node (SnarkJS / circomlib / scripts de witness/proof)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
*.tsbuildinfo
# package-lock.json se versiona para reproducibilidad de SnarkJS

# IDE y editores
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~

# Sistema operativo
.DS_Store
Thumbs.db

# Logs y temporales
*.log
tmp/
temp/
proof.json
public.json
input.json
!test/fixtures/**/input.json
!test/fixtures/**/proof.json
!test/fixtures/**/public.json
