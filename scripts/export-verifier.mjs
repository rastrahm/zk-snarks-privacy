/**
 * Stub Fase 0 — exporta Groth16Verifier.sol (implementacion en Fase 3).
 * Uso: `npm run export:verifier`
 */
console.log("[Fase 0 stub] export-verifier.mjs — pendiente Fase 3 (snarkjs zkey export solidityverifier).");
process.exit(0);
