/**
 * Stub Fase 0 — compila el circuito Circom (implementacion en Fase 2).
 * Uso: `npm run compile:circuit`
 */
console.log("[Fase 0 stub] compile-circuit.mjs — pendiente Fase 2 (withdraw.circom).");
console.log("Prereq: circom >= 2.1 instalado en PATH.");
process.exit(0);
